#include "../headers/couleur.h"

int main(void) {
    srand(time(NULL));

    FIFO f;
    coordonnees c;
    f = initFIFOVide();

    /*
    flux* f = openFile("test.txt");

    int size = checkFileFormat(f);
    Matrix M = initFromFile(f);
    printMatrix(M, size);

    close(f);
    */

}
